{
  "name": "slack-claude-channel",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "bun install --no-summary && bun server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@slack/bolt": "^4.0.0",
    "zod": "^4.3.6"
  }
}
